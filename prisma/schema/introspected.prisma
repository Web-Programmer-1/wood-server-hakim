// model shadhinota_images {
//   id                  String              @id
//   shadhinotaId        String
//   url                 String
//   sortOrder           Int                 @default(0)
//   createdAt           DateTime            @default(now())
//   shadhinota_sections shadhinota_sections @relation(fields: [shadhinotaId], references: [id], onDelete: Cascade)

//   @@index([shadhinotaId])
//   @@index([sortOrder])
// }

// model shadhinota_sections {
//   id                String              @id
//   title             String
//   subtitles         Json?
//   sortOrder         Int                 @default(0)
//   createdAt         DateTime            @default(now())
//   updatedAt         DateTime
//   shadhinota_images shadhinota_images[]
//   shadhinota_videos shadhinota_videos?

//   @@index([createdAt])
//   @@index([sortOrder])
// }

// model shadhinota_uploaded_videos {
//   key       String   @id
//   url       String
//   createdAt DateTime @default(now())
//   updatedAt DateTime
// }

// model shadhinota_videos {
//   id                  String              @id @default(uuid())
//   shadhinotaId        String              @unique
//   url                 String
//   createdAt           DateTime            @default(now())
//   shadhinota_sections shadhinota_sections @relation(fields: [shadhinotaId], references: [id], onDelete: Cascade)
// }
